"""
Includes all constants that the image generator will use.
Here, 'constants' are things that won't (typically) change in an image SEQUENCE, although they
may need to be changed for different sequences, in that case change the values here.
"""

HEIGHT = 1080
WIDTH = 1920

OUTPUT_DIR = "./images"
